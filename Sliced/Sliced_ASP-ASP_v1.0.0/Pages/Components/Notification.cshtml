@page
@model Sliced.Pages.Components.NotificationModel
@{
    // This block allows server-side C# code execution for dynamic content and logic.

    ViewData["HeadTitle"] = "Components";
    ViewData["ChildTitle"] = "Notification";
    ViewData["x-data"] = "components";
    ViewBag.ActiveItem = "notification";
}

<!-- Start All Card -->
<div class="flex flex-col gap-4 min-h-[calc(100vh-212px)]">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div class="bg-white border border-black/10 p-5 rounded dark:bg-darklight dark:border-darkborder">
            <h2 class="text-base font-semibold text-black dark:text-white/80 mb-4 capitalize">Basic Notification</h2>
            <div x-data="{ toastVisible: false, toastTimer: null }">
                <div x-clock x-show="toastVisible" class="bg-black text-white py-3 px-4 rounded-md max-w-[220px] fixed bottom-5 right-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                    <span>Hello, world! This is a toast message.</span>
                </div>
                <button class="btn bg-purple border border-purple rounded-md text-white transition-all duration-300 hover:bg-purple/[0.85] hover:border-purple/[0.85]" x-on:click="
                        if (toastVisible) {
                        clearTimeout(toastTimer);
                        toastTimer=setTimeout(() =>
                    toastVisible = false, 3000);
                    } else {
                    toastVisible = true;
                    toastTimer = setTimeout(() => toastVisible = false, 3000);
                    }
                    ">
                    Open Notification
                </button>
            </div>
        </div>
        <div class="bg-white border border-black/10 p-5 rounded dark:bg-darklight dark:border-darkborder">
            <h2 class="text-base font-semibold text-black dark:text-white/80 mb-4 capitalize">Notification With Close</h2>
            <div x-data="{ notificationOpen: false }">
                <button x-on:click="notificationOpen = true" class="btn border dark:text-white dark:hover:text-black text-black border-light rounded-md transition-all duration-300 hover:bg-light hover:text-black">Click Me!</button>
                <div x-show="notificationOpen" class="bg-black flex items-start gap-3 text-white py-3 px-4 rounded-md max-w-[250px] fixed bottom-5 right-5 z-[99999]" x-cloak x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                    <span>Hello, world! This is a toast message.</span>
                    <button type="button" x-on:click="notificationOpen = false" class="">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" class="w-5 h-5">
                            <path d="M12.0007 10.5865L16.9504 5.63672L18.3646 7.05093L13.4149 12.0007L18.3646 16.9504L16.9504 18.3646L12.0007 13.4149L7.05093 18.3646L5.63672 16.9504L10.5865 12.0007L5.63672 7.05093L7.05093 5.63672L12.0007 10.5865Z" fill="currentColor"></path>
                        </svg>
                    </button>
                </div>
            </div>
        </div>
        <div class="bg-white border border-black/10 p-5 rounded dark:bg-darklight dark:border-darkborder">
            <h2 class="text-base font-semibold text-black dark:text-white/80 mb-4 capitalize">Position Notification</h2>
            <div class="flex flex-wrap items-center gap-4">
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-black text-white py-3 px-4 rounded-md max-w-[220px] fixed top-5 left-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border dark:text-white dark:hover:text-black text-black border-light rounded-md transition-all duration-300 hover:bg-light hover:text-black" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 3000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        Top Left
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-black text-white py-3 px-4 rounded-md max-w-[220px] mx-auto fixed top-5 left-0 right-0 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border dark:text-white dark:hover:text-black text-black border-light rounded-md transition-all duration-300 hover:bg-light hover:text-black" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 3000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        Top Center
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-black text-white py-3 px-4 rounded-md max-w-[220px] fixed top-5 right-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border dark:text-white dark:hover:text-black text-black border-light rounded-md transition-all duration-300 hover:bg-light hover:text-black" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 3000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        Top Right
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-black text-white py-3 px-4 rounded-md max-w-[220px] fixed bottom-5 right-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border dark:text-white dark:hover:text-black text-black border-light rounded-md transition-all duration-300 hover:bg-light hover:text-black" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 3000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        Bottom Right
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-black text-white py-3 px-4 rounded-md max-w-[220px] mx-auto fixed bottom-5 right-0 left-0 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border dark:text-white dark:hover:text-black text-black border-light rounded-md transition-all duration-300 hover:bg-light hover:text-black" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 3000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        Bottom Center
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-black text-white py-3 px-4 rounded-md max-w-[220px] fixed bottom-5 left-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border dark:text-white dark:hover:text-black text-black border-light rounded-md transition-all duration-300 hover:bg-light hover:text-black" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 3000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        Bottom Left
                    </button>
                </div>
            </div>
        </div>
        <div class="bg-white border border-black/10 p-5 rounded dark:bg-darklight dark:border-darkborder">
            <h2 class="text-base font-semibold text-black dark:text-white/80 mb-4 capitalize">Notification Color</h2>
            <div class="flex flex-wrap items-center gap-4">
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-purple text-white py-3 px-4 rounded-md max-w-[220px] fixed bottom-5 right-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border text-purple border-purple rounded-md transition-all duration-300 hover:bg-purple hover:text-white" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 3000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        Primary
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-info text-white py-3 px-4 rounded-md max-w-[220px] fixed bottom-5 right-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border text-info border-info rounded-md transition-all duration-300 hover:bg-info hover:text-white" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 3000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        Info
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-success text-white py-3 px-4 rounded-md max-w-[220px] fixed bottom-5 right-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border text-success border-success rounded-md transition-all duration-300 hover:bg-success hover:text-white" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 1000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        Success
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-warning text-white py-3 px-4 rounded-md max-w-[220px] fixed bottom-5 right-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border text-warning border-warning rounded-md transition-all duration-300 hover:bg-warning hover:text-white" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 1000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        Warning
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-danger text-white py-3 px-4 rounded-md max-w-[220px] fixed bottom-5 right-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border text-danger border-danger rounded-md transition-all duration-300 hover:bg-danger hover:text-white" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 1000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        danger
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-black text-white py-3 px-4 rounded-md max-w-[220px] fixed bottom-5 right-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border dark:text-white dark:hover:text-white dark:border-darkborder dark:hover:border-black text-black border-black rounded-md transition-all duration-300 hover:bg-black hover:text-white" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 1000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        black
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-muted text-white py-3 px-4 rounded-md max-w-[220px] fixed bottom-5 right-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border text-muted border-muted dark:border-darkmuted dark:text-darkmuted dark:hover:text-white rounded-md transition-all duration-300 hover:bg-muted hover:text-white" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 1000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        Secondary
                    </button>
                </div>
                <div x-data="{ toastVisible: false, toastTimer: null }">
                    <div x-clock x-show="toastVisible" class="bg-light text-black py-3 px-4 rounded-md max-w-[220px] fixed bottom-5 right-5 z-[99999]" x-transition:enter="transition ease-out duration-300" x-transition:enter-start="opacity-0" x-transition:enter-end="opacity-100" x-transition:leave="transition ease-in duration-300" x-transition:leave-start="opacity-100" x-transition:leave-end="opacity-0">
                        <span>Hello, world! This is a toast message.</span>
                    </div>
                    <button class="btn border dark:text-white dark:hover:text-black text-black border-light rounded-md transition-all duration-300 hover:bg-light hover:text-black" x-on:click="
                            if (toastVisible) {
                            clearTimeout(toastTimer);
                            toastTimer=setTimeout(() =>
                        toastVisible = false, 1000);
                        } else {
                        toastVisible = true;
                        toastTimer = setTimeout(() => toastVisible = false, 3000);
                        }
                        ">
                        light
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- End All Card -->